# design-reference/

Aquí vive el **prototipo v3** firmado por TAR (PRD §7.0) y el **logo** de marca, que
son la referencia obligatoria del frontend (Fase B).

> ⚠️ **Pendiente del cliente (Fase 1).** El prototipo v3 está pendiente de revisión
> y firma de TAR (hasta 3 rondas, §13). Hasta entonces esta carpeta solo contiene este
> placeholder. **No se construye la Fase B hasta que exista la versión firmada.**

Cuando llegue:

- Copia el HTML autocontenido del prototipo v3 a `design-reference/prototipo-v3/`.
- Copia el logo (con el rojo de marca **#D2103E**) a `design-reference/logo/`.
- NO copies los estilos inline, Leaflet ni las CDNs del prototipo: el look se replica
  con Tailwind y el mapa real es Google Maps (CLAUDE.md → Diseño).
