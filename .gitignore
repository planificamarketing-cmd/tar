# Dependencias
node_modules/
.pnpm-store/

# Entorno y secretos (NUNCA commitear)
.env
.env.*
!.env.example

# Builds
.next/
dist/
build/
*.tsbuildinfo

# Turborepo
.turbo/

# Artefacto de deploy del prototipo (se regenera)
/tar-prototipo-v3.zip

# Media local en dev (las imágenes no van al repo)
/srv/tar/media/
apps/api/uploads/

# Datos del cliente (PII): inventario real, aviso de privacidad, referencias.
# Disponibles localmente para el importador (Fase A.5) y contenido; NO van al repo.
/data/

# Playwright: resultados, capturas y reportes de las pruebas E2E (se regeneran)
test-results/
playwright-report/
blob-report/
.playwright/

# Respaldos y temporales
/var/backups/
/snapshots/
*.log
.DS_Store
